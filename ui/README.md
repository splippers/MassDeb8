# massdeb8 Chair UI (React)

The **Symposium of Infinite Contention** interface: title screen → lobby → arena, with **Control Knobs** for advanced settings.

See the repo root [`README.md`](../README.md) for dev/prod commands (`npm run dev`, `npm run build`, uvicorn).
